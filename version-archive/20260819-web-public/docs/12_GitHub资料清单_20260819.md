# GitHub 资料清单：20260819 xjaisop

本目录是 2026-08-19 的宁波 SOP 平台交付归档，包含当前工作区中可公开、可迁移的完整工程资料：

- `server.py`、`web/`、`config/`、`scripts/`、`qa/`：服务端、网页、配置、处理脚本和验证报告；
- `docs/`、`installer/`、启动脚本和 `requirements.txt`：部署、Windows/DGX 交付和运行说明；
- `web/media/`、`web/snapshots/`、`web/data/`、`datasets/`、`analysis/`：测试视频、快照、逐帧数据、预标注和分析结果；
- `models/`：GitHub 单文件限制内的模型权重和 ONNX 候选。

## 明确排除

- `.venv/`、`__pycache__/`、运行缓存、微信/系统缓存和本机用户数据库；
- 原始交付 ZIP，因为它们会重复占用空间，且不利于在线浏览；源码和模型已解包归档；
- `models/mobileclip2_b.ts`（约 254 MB），超过 GitHub 单文件 100 MB 限制；需要该文件时使用原始本地交付包或 Git LFS/Release；
- `runtime/camera_capture/` 中的现场录制证据，避免把本机运行状态误当成固定工程输入。

## 网络摄像头 3 当前状态

身份已核实为 `192.168.1.135` / `c4:3c:b0:be:40:e8`，554 端口可达，但设备对根地址和常见路径返回 `400 Bad Request`。工程不会伪造厂家 RTSP 路径；请从摄像头 App 复制完整地址，先运行：

```bash
./.venv/bin/python scripts/probe_network_camera.py 'rtsp://用户名:密码@192.168.1.135:554/厂家实际路径'
```

探测返回 `ok: true` 后，再在网页绑定区测试并保存。服务端已增加 TCP 可达性检查、FFmpeg TCP RTSP、账号脱敏错误信息和清晰的网络排查提示。

提交标识：`20260819 xjaisop资料`。
